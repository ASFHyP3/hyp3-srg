def test_back_projection(script_runner):
    ret = script_runner.run('back_projection', '-h')
    assert ret.success


def test_back_projection(script_runner):
    ret = script_runner.run('back_projection', '-h')
    assert ret.success
