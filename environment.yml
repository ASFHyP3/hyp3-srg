name: hyp3-srg
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.10
  - pip
  # For packaging, and testing
  - setuptools
  - setuptools_scm
  - wheel
  - pytest
  - pytest-console-scripts
  - pytest-cov
  - asf_search
  - hyp3_sdk
  # For running
  - hyp3lib>=3,<4
  - s1_orbits
  - pip:
    - -r requirements-static.txt
